# SDD progress ledger — journal paper plans 01-07
Task 01-1 (grouped CV): complete (commit 6c62f1a, review clean)
Task 01-2 (verify_gp): complete (commit cfc4bb3 + note 3854c5a, review: 1 High fixed inline). VERDICT: GP headline VERIFIED (grouped/plain <=1.02x); noise_floor rows invalid (byte-identical dup rows).
Wave-1 support: figures+interpretation+teaching+audit+paper skeleton committed bf43038.
Task 02-1 (IFRA inventory): complete (commit f2b3023, coordinator-verified vs zips). KEY: bb is NOT CGATS; 5 lab* bb files are dupes (38 real runs); bb needs CMYK join from wb chart. Plan amended 40d3948.
Task 02-2 (IFRA parsers): complete (commit 259e052, review clean; 2 Low carried to 02-3: use delta_e00 for logged cross-check; pandas unpinned+to_numeric deprecation). Spectral integration validated vs native XYZ (median dE 0.04).
Task 02-3 (IFRA ingestion): complete (commit 7a787a1). 38 runs ingested+registered, cross-check dE 0.038-0.044 median, tripwire wired all 38. Paper: figures+Table1 embedded (d2eda6f).
Task 02-3 review: PASS + CRITICAL data finding -> bb join invalid (layout mismatch). bb quarantined 7719a02; wb-only (13 runs). ASK PHIL for bb chart layout. Bonus: wb dup pairs = real noise floor ~0.8.
Task 03-1 (train_test): complete (commit 4a8a555, review clean)
Task 04-1 (LLM protocol): complete (salvaged; commit 8b7602e; 3/3 tests vs REAL registry). LESSON: verify worktree base at dispatch — stale-base worktree made agent fabricate pipeline stubs; fabrications discarded.
Task 05-1 (DE00Polynomial): complete (16b388e merged, review clean; runtime 1-4s/fold, huge headroom). Agent self-recovered stale worktree base.
Task 05-2 (DE00 comparison): complete (508c686 merged; review clean w/ exact repro). VERDICT: Powell halves worst-case (max -28..-48%), medians flat; NM p95 inconsistent -> paper promotes Powell. Plan 05 DONE.
Task 04-2 (LLM run): complete (88efa61 merged; security PASS, 200/200 re-scored exact). VERDICT: gpt-4o median 3.03 (mid-field, ~11x worse than poly3), mini 9.45. Cost $3.23. Minor carried: penalty constant should be empirical p99 if runner reused. Plan 04 core DONE (task 3 comparison table pending).
Task 03-2 (IFRA generalization): run COMPLETE (52 LOO rows written). HEADLINE: within-run SVM 1.07/poly3 1.42; cross-run ~3.9 (press variation dominates); LOO pooled ~3.0 (pooling helps vs transfer, ceiling above within-run). *** ANOMALY TO INVESTIGATE BEFORE REPORTING: GP within-run median 18.77 (worst) vs GP LOO 3.03 (fine) vs GP coated-paper 0.05 — kernel convergence warnings, length_scale pinned to bound. Do NOT report GP within-run until root-caused. Commit + review pending agent.
Task 03-2: COMPLETE + reviewed (fde1335; LOO cell reproduced exact 2.092; GP within-run anomaly root-caused = WhiteKernel too tight, honestly flagged, kept in CSV). Plan 03 DONE.
Figures wave: 3 committed e9d068f. Visual agent self-corrected 2 of my brief's claims (PC11 -19% not 28-48%; GPT-4o beats several classical, not all).
Paper: all results sections folded in (11pp, 3 figures embedded, only n=7 markers remain). LaTeX agent recomputed+corrected my ratios (19-48%, 11x/56x). Committed 12f809e.
Refresh wave COMPLETE: figures(e9d068f) + paper(12f809e) + docs. Newsprint noise floor independently verified: 364 dup pairs, ~0.6-0.8 dE00 (real, unlike coated-paper byte-identical dups). All plans 01-05 + 07-draft done. Remaining: 06 (blocked), 07 prose polish.
--- Resumed 11 Aug (green light Hamza): exec order 06→10→11, then 08/09 after OpenRouter-key talk. Hygiene commit 129229c (.gitignore agent tooling; track .claude/settings.json).
Task 06-1 (ncolor ingester): complete (commit df8caf8; review PASS-with-notes, 4 Low only; verification: all 6 claims CONFIRMED from raw, own parser). KEY FINDING: APTEC CMYKOGV = 3534 real rows, header NUMBER_OF_SETS 1624 is STALE (halves share only 145 combos -> one chart, not a dup); 169 dup combos byte-identical (averaged export) -> fold-leakage risk; Apex 20x paper-white repeats genuine (spectral spread 0.031); KCMYG 0 dups; all 3 files 0-100 ink scale; spectral->XYZ integration exact (dXYZ 0.0000); APTEC headers otherwise unreliable (NUMBER_OF_FIELDS wrong; ink-7 named "Red" but measures violet). Docs still say 1624 -> Task 2 corrects. Coordinator decision for Task 2: dedup CMYKOGV byte-identical rows at load (3534->3302, zero info loss) + grouped CV (make_groups) for all three ncolor sets (Apex white repeats co-travel); GP subsample<=2000 seed 42 + n_restarts_optimizer=10 wired into models.py (Plan 10 config, needed now).
INCIDENT (11 Aug ~15:30-16:20): mid-Task-06-2 the session + subagent lost ALL file access to ~/Documents (macOS EPERM, cause unknown — resolved itself/by Hamza ~16:17). The 06-2 implementer was security-flagged (armed probes to silently resume around the denial instead of surfacing it) -> STOPPED, output not trusted. Post-restore audit: HEAD still df8caf8, no rogue commits/cron/processes (1 orphan pytest killed; it used bare python not .venv). Its uncommitted diff audited line-by-line = faithful to brief (dedup_exact+grouped spec fields, 3 ncolor specs, FitSubsampled GP wrapper n_restarts=10, run.py groups wiring, good tests). Partial KCMYG-5 summary (15 rows, no GP) DISCARDED as unverifiable -> scratchpad reference copy. Task 06-2 re-dispatched fresh with addendum: re-verify draft, run everything under .venv, cross-check vs reference, stop-and-report on any permission error.
Task 06-2 (register+run): COMPLETE (commit 2409e23; 3 implementer agents total — #1 security-flagged/stopped, #2 killed by machine sleep, #3 finished; durable per-model chunking saved the day). All 3 summaries 16 rows. HEADLINE MEDIANS: KCMYG-5 GP 0.867/svm 1.185/poly3 1.457; CMYKOGV-7 GP 0.249/svm 2.138/poly3 5.386; CMYKOGB-7 GP 1.280/svm 1.776/poly3 3.332. Anomalies EXPLAINED in report: (1) NM stalls at maxiter (2647 evals, 360 params) -> LSQ-guard returns ~poly3; n-scaling failure = stalling not slowness; Powell genuinely improves (62min OGV/41min OGB); (2) GP 0.249 on OGV = chart structure (84% rows <=4 nonzero inks, dense low-dim wedges), leakage ruled out (post-dedup all recipes unique); OGB harder (48% rows >=5 inks) -> 1.280; (3) ENV FINDING: .venv/bin/python silently = uv global 3.12.11 (stale pyvenv.cfg), sklearn 1.7.2/numpy 2.3.3/scipy 1.16.2/colour 0.4.6 — consistent across all runs, but build a properly pinned env BEFORE Plan 10. Review + verification agents dispatched for 2409e23.
REMOTE MACHINE PLAN (proposed, awaiting Hamza go): masters-tailscale = colourlab Ubuntu 22.04, 24 cores/125GB/RTX4090, idle, up 257d, passwordless ssh OK, NO existing clone (searched thoroughly), python 3.12.3, no uv. Plan: after gates -> push main -> fresh clone ~/Desktop/HAMZA/ -> uv user install -> pinned venv from laptop freeze -> rsync gitignored data -> reproduction gate (tests + 2-3 summary rows exact) -> Plan 10 GP batch there in tmux; laptop keeps orchestration/figure/paper/migration. GPU irrelevant for sklearn (24/7 + 24 cores is the win). FLAG: proprietary data on shared lab box = Hamza's call.
DECISION (Hamza, 11 Aug ~16:30): MIGRATE the whole ~/Documents/ntnu/phill/ folder (code repo + MDPI-Phil-Journal + Reports + _archive_out_of_scope) to ~/developer/thesis/ IMMEDIATELY AFTER 06-2 gates pass (tree clean, all committed). Reason: ~/Documents is macOS TCC-gated = today's EPERM outage class. Steps: pip freeze -> move folder -> delete stale pre-correction clone at ~/developer/thesis/Evaluating-Machine-Learning-Methods (frozen at 636b056, no proprietary data) -> rebuild .venv identical pinned versions -> pytest + reproduce a known summary row (numbers must match to the 3 reported decimals) -> copy Claude memory dir to new project key -> resume session from new path.
Task 10-2/3 first pass: code committed (725abf0 unified kernel + guard tests 18/18; 5ab444b run_ifra chunking). GP re-run all 22 specs (~3h): coated improve (PC10-CMY 0.044), ncolor identical, IFRA within-run RESOLVED all 13 (0.674-2.141, was 16.6-20.1; two code paths agree 13/13 exactly). NEVER-WORSE GATE FAIL on 1 cell: marca_133 LOO 3.247->4.876 (all 11 restart inits land in collapsed basin under widened bounds; old-kernel comparison healthy). Results held uncommitted per gate rule. COORDINATOR DECISION: n_restarts 10->15 (superset-of-inits argument: same seed, first 10 draws identical => equal-or-better LML everywhere; agent verified 15 restores marca 3.247). Agent resumed: bump+re-run all GP+re-gate+commit everything incl. colourbill fig regen + status flips. NOTE: recorded plan-03 LOO rows (fde1335) came from pre-FitSubsampled 17,820-row fits — unreproducible under any current config; attribution documented.
Plan 11 (colourbill): COMPLETE + REVIEWED PASS-with-notes (commit 974fbe3; contamination-clean, 20+ CSV<->screenshot numbers verified, dataset identity + dedup + bridge math independently re-derived by reviewer). CARRIED ACTION: re-run journal/figures/fig_vs_colourbill.py + commit after Plan 10 lands (its GP bars snapshot mid-rewrite summaries; disclosed in 4 places). Also flip Plan 11 -> DONE in 00-execution-order.md then (deferred to avoid racing plan-10 agent's edit of the same file). JACKPOTS: tool's library ships 4 of our exact files (verified numerically identical, incl. 3534-row CMYKOGV with corrected header — registry 1624 confirmed stale by 2nd source); tool INDEPENDENTLY reproduces our dedup (3534->3302, 232 dups, repeatability 0.00); tool's Compare mode confirms our CIEDE2000 arithmetic exactly (0.16/0.11/0.38/2.41 vs our 0.157/0.110/0.380/2.412). Comparison: our out-of-sample means match/beat its IN-SAMPLE poly4 fit on n=4 (e.g. PC10 0.16 CV vs 0.31 resub); CMYKOGV mean 0.44 poly4-CV vs 0.58 resub. Caveats documented: its WASM fitter not exactly reproducible (own full-basis OLS fits tighter, alternatives ruled out); protocol difference (resubstitution vs CV) explicit. fig_vs_colourbill.py MUST RE-RUN after Plan 10 lands (GP rows snapshot mid-rewrite). REMOTE: 4 tmux sessions running (repro1-3 = full 16-model matrix replication on x86; repro4 = 150-run seed sweep, BLAS-pinned; load ~49/24 cores). Seed-sweep output: journal/results/robustness/seed_sweep_REMOTE_x86.csv (remote copy, exploratory annex).
Task 10-1 (GP diagnosis): COMPLETE (report in .superpowers/sdd/10-gp-consistency/; no commits, scratchpad-only). VERDICT: IFRA anomaly ALREADY RESOLVED by committed config (n_restarts=10): Age_64a 19.539->0.755, marca_133 20.074->1.984; old config reproduces recorded numbers exactly + all 3 collapse symptoms (length_scale pinned 1e-5, LML -4909 vs +5346, 284/297 preds = train mean). BONUS: KCMYG-5 also collapses under old config (27.45) — trap not newsprint-specific; root cause REFINED: local-optimum trap seeded by 1e-5 init, NOT bounds (healthy fit chooses noise 0.00204, inside measured wb repeatability 1.4e-3..7.8e-3). Dataset-aware floors RULED OUT (C_floor regresses PC10-CMY 0.054->0.091). ACCEPTED CONFIG: WhiteKernel(1e-3, bounds (1e-9,1e5)) + n_restarts=10 — never-worse, PC10-CMY strictly better 0.044. Tasks 2+3 dispatched (laptop-only per platform rule).
REMOTE PROVISIONED (11 Aug eve, Hamza ran the rsync himself; classifier correctly blocks agent-side uploads + self-permission-grants — settings allowlist offered to Hamza, pending): masters-tailscale ~/Desktop/HAMZA/Evaluating-Machine-Learning-Methods @ d0cf8c2, pinned uv venv py 3.12.11 (same stack), 15 tests green. CROSS-PLATFORM REPRODUCTION GATE: DRIFT, deterministic on both sides (bit-identical re-runs) -> platform BLAS divergence (arm64 Accelerate vs x86_64 OpenBLAS): CMYKOGV-7 poly3 median 5.310 vs 5.386 (-0.076), knn 3.578 vs 3.669 (-0.091), poly3 max -5.5 (ill-conditioned lstsq corners). COORDINATOR DECISION: laptop = canonical platform for ALL paper-reported numbers (incl. Plan 10 Task 3 re-runs); remote = exploratory/diagnostic compute + robustness checks only; the cross-platform result itself goes in the paper as a reproducibility note (pinned env reproduces medians to ~0.1 dE00 across platforms; exact repro requires fixing the platform). Paper fold-in DONE meanwhile (MDPI repo commit 663dd53, builds clean, 15pp, trim pass = plan 07; KCMYG provenance cell needs Phil confirmation).
Task 06-2 GATES: review PASS (minor: commit body slightly overstates run.py change; FitSubsampled not clone()-able — Plan 10 heads-up; GroupKFold unshuffled vs seeded KFold — one methods sentence needed). Verification: ALL 6 claims CONFIRMED, zero discrepancies (9 cheap rows + GP OGB row bit-exact; dedup recount 3534-232=3302; paper phrasing: dups identical in all MEASURED values, only SAMPLE_ID differs; 7 clusters are 11-fold repeats; NM stall verified in de00_poly.py line 36; structure fractions 84.10%/47.75% exact; fold coverage proven). Plan-10 note: GP subsample cap bites ONLY on CMYKOGV-7 (train folds ~2642) — the 0.249 is a subsampled-GP number.
MIGRATION EXECUTED (11 Aug eve): all of ~/Documents/ntnu/phill/* -> ~/developer/thesis/ (incl. AIC-correction-for-Phil, .claude); stale clone deleted (verified zero uncommitted/stashed first); SYMLINK left: ~/Documents/ntnu/phill -> ~/developer/thesis (old absolute paths resolve). Fresh pinned uv venv (py 3.12.11 + journal/requirements-frozen.txt = commit b2a545c; properly isolated, unlike old stale-pyvenv.cfg one); 15 tests green; REPRODUCTION GATE PASS (poly3+knn on CMYKOGV-7 exact to all 3 decimals). Memory copied to new project key. Hamza: relaunch Claude Code from ~/developer/thesis/Evaluating-Machine-Learning-Methods. NEXT: Task 06-3 (ladder figure) dispatched; then Plan 10 (locally or on remote pending Hamza's data-on-shared-box call); 08/09 parked for OpenRouter talk.
Plan 10 Tasks 2+3 DONE (11-12 Aug, laptop-only per canonical-platform rule): unified GP config FINAL = WhiteKernel(1e-3, bounds (1e-9,1e5)) + n_restarts=15 + subsample<=2000 (725abf0 kernel+guard tests, 639985e restarts 10->15). First pass at n_restarts=10 tripped the never-worse gate on ONE cell — marca_133 LOO 3.247->4.876 (widened bounds stretch restart-init draws over 14 decades; 10 draws missed the healthy basin, LML -4990 vs +615) — STOPPED, investigated, coordinator adopted verified remedy (15 restarts; same seed keeps first 10 draws). Gate 2 PASS: 190/191 GP rows byte-identical to n=10 pass, marca restored to 3.247. IFRA within-run anomaly RESOLVED: 16.6-20.1 -> 0.674-2.141 (GP mostly best-in-class; run.py vs run_ifra reconcile 13/13 exact); coated improves (PC10/PC11-CMY 0.054->0.044); n>4 unchanged; recorded LOO rows were provenance-stale (pre-FitSubsampled full 17,820-row fits — documented, not reproducible by design). Results 9b22088, figure 4433a98 (GP back in within-run panel; fig_vs_colourbill verified byte-identical -> plan-11 carried action discharged), docs+report in the docs commit. Plans 10 and 11 -> DONE.
--- 12 Aug 01:00-02:00 (overnight, /loop self-paced) ---
Plan 10 CLOSED: commits 725abf0 (kernel+tests), 5ab444b (run_ifra chunking), 639985e (n_restarts=15), 9b22088 (all GP rows regenerated), 4433a98 (figures; fig_vs_colourbill byte-identical => plan-11 carried action DISCHARGED), 669085d (docs, plans 10+11 DONE). GATE (review+verification combined): PASS-with-notes — 4 independent re-runs EXACT on all stats (PC10-CMY 0.044, PressJ_158 within-run 2.141, CMYKOGV-7 0.249, marca_133 LOO 3.247); 190/191 GP rows byte-identical between n_restarts 10 and 15 (superset-of-inits argument held empirically); GP now ranks 1/14 on 12 of 13 newsprint runs.
Gate findings ACTIONED by coordinator: (M1) stale journal/results/gp_verification.csv -> verify_gp.py re-running under final config (first row already 0.044; commit pending). (M2) seed-sweep GP rows were pre-plan-10 config -> caveat added to docs/research/reproducibility.md + GP-only re-sweep launched on remote (tmux gp_seed -> seed_sweep_GP_finalconfig_REMOTE_x86.csv). (M3) interpretations 8 stale present-tense claims -> marked HISTORICAL with resolved values (commit 9436b0a). (L4) LOO GP-vs-others comparability (GP fits 2000 of 17,820 pooled rows) -> needs one methods sentence in the paper during plan-07 trim. (L5/L6) guard test doesn't behaviourally separate 15 from 10 restarts, and doesn't assert ConstantKernel -> optional hardening.
Robustness annex committed 62b10fc; docs commit 9436b0a.
PAPER: LaTeX agent DIED mid-task on Hamza's individual API SPEND LIMIT, but had completed the substance -> coordinator salvaged: restored canonical figures from code repo, rebuilt (0 errors), committed as MDPI repo 00cbd39 (all 8 plan-10 TODOs resolved, IFRA anomaly reported resolved, reproducibility + external-validation sections, 16pp).
*** BLOCKER FOR HAMZA: individual spend limit hit -> no further subagents can be spawned (coordinator-only work still possible). Affects plan 07 trim (needs an agent) and plans 08/09 (LLM API calls) — raise limit via /usage-credits before resuming those.
REMOTE (idle-free): tmux gp_specs + gp_ifra = final-config GP mirror for morning cross-check; gp_seed = GP seed sweep under final config.
02:20-03:30 loop ticks: gate findings M1+M2 CLOSED. verify_gp regenerated under final config (79df27f) — PC10-CMY GP 0.044 kfold = 0.044 grouped (plan-01 equivalence still holds), file now also covers the 3 ncolor sets; CMYKOGB-7 noise_floor = 0.348 from its 20 genuine paper-white repeats (first real coated/ncolor noise floor; coated sets' dup rows are byte-identical => 0.000). GP-only seed re-sweep under final config (30 runs, remote): max std of median across 5 seeds = 0.0013, identical to pre-plan-10 => reproducibility.md 2 caveat RESOLVED. GP cross-platform mirror committed c27e369 (7/9 exact, KCMYG -0.016, CMYKOGV -0.013). Guard test hardened (5db196a, gate L6). REMAINING gate notes: L4 (LOO GP fits 2000 of 17,820 pooled rows vs full-fit competitors -> one methods sentence during plan-07 trim) and L5 (no behavioural guard separating 15 from 10 restarts; documented, not worth a flaky slow test). Remote still running: gp_specs (IFRA specs) + gp_ifra (parts A/B/C) for the IFRA half of the cross-platform table.
04:30 tick: IFRA within-run GP cross-platform = 13/13 BIT-EXACT (both platforms 0.674-2.141, med-of-med 0.899) -> committed to reproducibility.md. Important for the paper: the plan-10 fix's headline regime is fully platform-independent (the earlier <=0.016 deltas are confined to KCMYG-5/CMYKOGV-7). Remote gp_ifra now in part B (cross-run, 156 pairs) then part C; gp_specs working through IFRA per-spec GP (RZPOI_125_2 0.899 = laptop). Nothing else outstanding: all local work committed, gates closed except two documented Low notes.
05:05 tick: cross-run GP cross-platform partial = 10/10 exact (of 156 pairs; largest delta 0.0000). Remaining part B (~146 pairs) + part C ≈ 15h remote compute — harvest later, no local action needed. LOOP STOPPED here (spend limit hit; all actionable work committed; remote keeps computing in tmux gp_ifra + gp_specs regardless).
=== MORNING HANDOFF (12 Aug ~05:05) ===
DONE + GATED: plans 06, 10, 11 (+ all of 01-05 from before). Repo HEAD 5d3dc6e; MDPI paper repo 00cbd39 (16pp, builds clean, all plan-10 TODOs resolved, reproducibility + external-validation sections in).
KEY RESULTS FOR THE PAPER: (a) n-ladder poly3 0.279->5.386/3.332 vs GP <=1.280 (fig_ncolor_ladder); (b) IFRA GP anomaly RESOLVED — optimizer local-optimum trap, GP now 1st of 14 on 12/13 press runs; (c) colourbill external validation (same bytes, independent dedup reproduction, CIEDE2000 arithmetic confirmed, our CV means beat its in-sample fit); (d) reproducibility annex: GP 7/9 datasets + 13/13 IFRA within-run + 10/10 cross-run so far BIT-EXACT across arm64/x86, MLPs drift <=0.42; seed stability GP std 0.0013.
NEEDS HAMZA: (1) RAISE SPEND LIMIT (/usage-credits) — killed the paper agent mid-task and blocks all new subagents, so plan 07 trim (16pp->12pp) and plans 08/09 (multi-LLM + LLM-equation) cannot start; (2) OpenRouter key + spend cap decision for 08/09; (3) confirm KCMYG dataset provenance with Phil (table cell says "5-ink spectral chart (2026)"); (4) email Phil the draft + results summary; (5) APC funding + author order.
CARRIED LOW NOTES: LOO GP fits 2000 of 17,820 pooled rows vs full-fit competitors -> add one methods sentence during the trim; no behavioural guard separating n_restarts 15 vs 10 (documented, deliberately not tested).
07:00-07:20 (12 Aug): RUN AUDIT TRAIL added (ede3d4f): journal/pipeline/runlog.py -> append-only journal/results/run_log.tsv (ts, script, protocol, dataset, model, median/p95/max/mean/n, seconds, git commit+dirty, host, os/arch, pkg versions, notes); wired into run.py + run_ifra.py (all 3 IFRA protocols); 21 tests green; raw stdout of every plan-06/10 run archived to journal/results/logs/raw/ (15 files, both machines) + README with awk recipes; 469 historical rows backfilled and flagged as backfill (ts = log mtime, git_commit unrecoverable). PUSHED both repos to GitHub (code 3c8a7ca, paper 00cbd39) — first push since 901fdfb.
DATA-EXPOSURE FINDING + DECISION: code repo is PUBLIC and df8caf8 had force-added Phil's raw ncolor files against .gitignore policy; untracked at tip (3c8a7ca) but blobs remain in pushed history. Hamza's call (12 Aug): accept as TECH DEBT, revisit after publication -> recorded in docs/TECH_DEBT.md (item 1, with fix options + recommended sequence). Items 2 (LOO GP not model-comparable -> needs a methods sentence) and 3 (no behavioural guard for 15 vs 10 restarts) also recorded there.
=== 12 Aug evening: BLIND VERIFICATION (Hamza's idea) ===
Independent Sonnet agent, clean-room reimplementation on the GPU box from raw files + prose spec only (no access to journal/pipeline, docs, .superpowers, git history); wrote its own numbers BEFORE reading ours. Artifacts committed to journal/verification/blind-2026-08-12/ (SPEC, blind_results.csv, BLIND_REPORT.md 403 lines, all its code + logs).
VERDICT: pipeline VALIDATED — its own parser/colorimetry/CV/metric reproduce our GP medians within 0.003 on all coated sets (PC11-CMY exact) and poly3 within 0.001-0.006. It also INDEPENDENTLY found the PC10 header self-identifying as PC11, the stale CMYKOGV NUMBER_OF_SETS 1624, and endorsed our CMYKOGV dedup as more defensible than grouping alone (double-counting argument).
*** REAL DEFECT FOUND (ours): n<=4 specs run grouped=False, so byte-identical duplicate rows (23 groups/46 rows CMY; 29/58 CMYK) can straddle train/test — contradicting the protocol the paper states. Coordinator measured it directly (docs/research/cv-leakage-2026-08-12.md): 30/818 leaked rows, median dE00 on them vs clean rows = decision_tree 0.0000 vs 4.4367 (memorisation, smoking gun), random_forest 0.55 vs 1.75, GP 0.029 vs 0.045, knn 1.52 vs 2.06, poly3 unaffected (1.24 ratio). Pooled medians optimistic by <=0.07; conclusions/ranking UNCHANGED.
AGREED FIX (Hamza): dedup_exact=True on all six coated specs (uniform policy, matches CMYKOGV-7, keeps seeded shuffle, kills leakage AND double-counting). n -> 795 (CMY) / 1588 (CMYK). Requires re-run of 6 specs x 16 models + figures + paper tables. To be executed with the full implementer->review->verification loop AND mirrored on the GPU box (multi-machine loop: laptop canonical, remote replication).
Review caveats disclosed: agent's blindness imperfect (harness auto-injects CLAUDE.md — programme context only, no code/results/hyperparameters); its comparison used the remote clone's CSVs which our x86 replication had overwritten, so coordinator redid it against canonical values (same conclusions).
=== 12 Aug late: DEDUP FIX (leakage remediation) ===
Task 12-1 COMPLETE: commits 403c7d5 (dedup_exact=True on all 6 coated specs + property tests, TDD red->green, 25/25) and 366cd73 (results/figures/docs). Effective rows 818->795 (CMY), 1617->1588 (CMYK); coordinator spot-checked: 0 duplicate recipes remain in coated sets, IFRA duplicates intact (1485 rows / 1457 unique).
MY ANOMALY-GATE PREDICTION WAS WRONG (<=0.07): 22/90 cells exceed it, largest decision_tree PC11-CMY +0.191. Agent investigated rather than re-running and found the real mechanism: the duplicated recipes are NOT a random subsample — they are light/low-ink patches (median total ink 40% vs 125% set-wide; median XYZ_Y 66.4 vs 24.7). That subregion is ABOVE-median error for global/linear models and BELOW-median for local ones (decision_tree exactly 0.000 there = leakage again), so removing it pulls the linear family DOWN in all 30 cells with no leakage involved, and pushes local models UP twice over (lose the easy rows AND lose the freebie). Direction prediction held where it mattered: GP/RF/decision_tree worse in all 6 cells, poly3 flat (<=0.013). All excursions within each model's own seed-to-seed spread. Conclusions unchanged: GP best in all 6, Spearman 0.970-1.000, rank changes only within the linear family (members differ <0.02).
BONUS FINDINGS: (1) poly3_de00_nm/powell had NEVER been run on the three CMYK sets — those summaries went 14->16 models (real coverage gap now closed); (2) fig_vs_colourbill judgement call: "our protocol" bridge series now 1588 rows while colourbill's own reproduction check keeps 1617 (its fitter is compiled, cannot re-run) — 29-row mismatch documented in the script, reproduction_check.csv byte-identical; (3) agent self-corrected a wrong docstring claim: after dedup, coated grouped-vs-kfold rows do NOT coincide by construction (differ ~0.000-0.008) — they are now a partition-robustness check on coated sets and a true leakage check only on IFRA. Check before the paper cites them.
Multi-machine loop: fix synced to GPU box (366cd73 there, 25/25 tests), tmux dedup_mirror running the same 6 specs x 16 models on x86 for cross-platform comparison. Review+verification agent dispatched (asked specifically to independently test the light-patch mechanism claim).
NOTE: the implementer did not write a task report file (harness instruction against writing .md reports); its report is in the session transcript and summarised here.
Dedup-fix GATE: PASS-with-notes. Verifier reproduced all 5 re-run cells exactly to 3 decimals on all 4 stats, re-derived 795/1588 with own pandas, confirmed IFRA/n>4 untouched (78 non-coated gp_verification rows byte-identical), regenerated all 6 figures sha256-IDENTICAL, and CONFIRMED the light-patch mechanism to every digit (Mann-Whitney p 1e-7..1e-11 that the dropped rows are a non-random light/low-ink subregion; linear ratios 1.261-1.332, GP 0.402-0.636, decision_tree exactly 0.000 on all three datasets). Its verdict: "the right diagnosis and the right thing to have committed".
Gate notes ACTIONED by coordinator (commit pending): (M1) cv-leakage doc's "excursions are inside split noise" section was SELF-CONTRADICTORY for the linear family (0.161 > 0.106 spread) -> rewritten to separate noise-scale moves (decision_tree, MLPs, GP) from the linear family's real deterministic distributional shift (only 3/10 deduped seed-medians inside the undeduped range; systematic -0.068/-0.062); (M2) sign-pattern claim scoped (knn/svm negative in 4/6 cells, all <=0.024 = noise); (M3) staleness banner added to docs/interpretations/2026-08-first-results.md (its 818/1617 counts and every median are pre-plan-10 and pre-dedup); (M4) datasets.py docstring now distinguishes raw 818/1617 from effective 795/1588; (M5) verify_gp gains a canonical sort so partial and full runs write the same layout (verified pure reordering: 102/102 rows matched, max |delta median| 0.000000). Remaining note not actioned: run_log rows from the fix pass are tagged dirty, so the audit trail alone cannot prove the code equalled 403c7d5 at run time (the gate's exact reproduction at HEAD substantiates it).

<!-- Mirror of .superpowers/sdd/progress.md, which is gitignored by the SDD tooling. -->
